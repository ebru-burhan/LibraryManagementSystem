import React from 'react';

export default function DashboardPage() {
  return (
    <div>
      <h1 style={{ color: 'var(--text-main)' }}>Dashboard</h1>
      <p style={{ color: 'var(--text-muted)' }}>Sisteme hoş geldiniz.</p>
    </div>
  );
}